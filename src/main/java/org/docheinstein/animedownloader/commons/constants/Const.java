package org.docheinstein.animedownloader.commons.constants;

/** Contains constant that don't depend on the application. */
public class Const {
    public static class Math {
        public static final long K = 1000;
        public static final long M = K * K;
    }
}
